
(ns layers.Permute
  (:require [keras.layers.core.Permute]))

(defonce Permute keras.layers.core.Permute/Permute)


(defonce add-loss keras.layers.core.Permute/add-loss)

(defonce add-update keras.layers.core.Permute/add-update)

(defonce add-weight keras.layers.core.Permute/add-weight)

(defonce assert-input-compatibility keras.layers.core.Permute/assert-input-compatibility)

(defonce build keras.layers.core.Permute/build)

(defonce built keras.layers.core.Permute/built)

(defonce call keras.layers.core.Permute/call)

(defonce compute-mask keras.layers.core.Permute/compute-mask)

(defonce compute-output-shape keras.layers.core.Permute/compute-output-shape)

(defonce count-params keras.layers.core.Permute/count-params)

(defonce get-config keras.layers.core.Permute/get-config)

(defonce get-input-at keras.layers.core.Permute/get-input-at)

(defonce get-input-mask-at keras.layers.core.Permute/get-input-mask-at)

(defonce get-input-shape-at keras.layers.core.Permute/get-input-shape-at)

(defonce get-losses-for keras.layers.core.Permute/get-losses-for)

(defonce get-output-at keras.layers.core.Permute/get-output-at)

(defonce get-output-mask-at keras.layers.core.Permute/get-output-mask-at)

(defonce get-output-shape-at keras.layers.core.Permute/get-output-shape-at)

(defonce get-updates-for keras.layers.core.Permute/get-updates-for)

(defonce get-weights keras.layers.core.Permute/get-weights)

(defonce input keras.layers.core.Permute/input)

(defonce input-mask keras.layers.core.Permute/input-mask)

(defonce input-shape keras.layers.core.Permute/input-shape)

(defonce losses keras.layers.core.Permute/losses)

(defonce non-trainable-weights keras.layers.core.Permute/non-trainable-weights)

(defonce output keras.layers.core.Permute/output)

(defonce output-mask keras.layers.core.Permute/output-mask)

(defonce output-shape keras.layers.core.Permute/output-shape)

(defonce set-weights keras.layers.core.Permute/set-weights)

(defonce trainable-weights keras.layers.core.Permute/trainable-weights)

(defonce updates keras.layers.core.Permute/updates)

(defonce weights keras.layers.core.Permute/weights)
