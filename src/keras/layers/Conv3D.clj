
(ns layers.Conv3D
  (:require [keras.layers.convolutional.Conv3D]))

(defonce Conv3D keras.layers.convolutional.Conv3D/Conv3D)


(defonce add-loss keras.layers.convolutional.Conv3D/add-loss)

(defonce add-update keras.layers.convolutional.Conv3D/add-update)

(defonce add-weight keras.layers.convolutional.Conv3D/add-weight)

(defonce assert-input-compatibility keras.layers.convolutional.Conv3D/assert-input-compatibility)

(defonce build keras.layers.convolutional.Conv3D/build)

(defonce built keras.layers.convolutional.Conv3D/built)

(defonce call keras.layers.convolutional.Conv3D/call)

(defonce compute-mask keras.layers.convolutional.Conv3D/compute-mask)

(defonce compute-output-shape keras.layers.convolutional.Conv3D/compute-output-shape)

(defonce count-params keras.layers.convolutional.Conv3D/count-params)

(defonce get-config keras.layers.convolutional.Conv3D/get-config)

(defonce get-input-at keras.layers.convolutional.Conv3D/get-input-at)

(defonce get-input-mask-at keras.layers.convolutional.Conv3D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.convolutional.Conv3D/get-input-shape-at)

(defonce get-losses-for keras.layers.convolutional.Conv3D/get-losses-for)

(defonce get-output-at keras.layers.convolutional.Conv3D/get-output-at)

(defonce get-output-mask-at keras.layers.convolutional.Conv3D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.convolutional.Conv3D/get-output-shape-at)

(defonce get-updates-for keras.layers.convolutional.Conv3D/get-updates-for)

(defonce get-weights keras.layers.convolutional.Conv3D/get-weights)

(defonce input keras.layers.convolutional.Conv3D/input)

(defonce input-mask keras.layers.convolutional.Conv3D/input-mask)

(defonce input-shape keras.layers.convolutional.Conv3D/input-shape)

(defonce losses keras.layers.convolutional.Conv3D/losses)

(defonce non-trainable-weights keras.layers.convolutional.Conv3D/non-trainable-weights)

(defonce output keras.layers.convolutional.Conv3D/output)

(defonce output-mask keras.layers.convolutional.Conv3D/output-mask)

(defonce output-shape keras.layers.convolutional.Conv3D/output-shape)

(defonce set-weights keras.layers.convolutional.Conv3D/set-weights)

(defonce trainable-weights keras.layers.convolutional.Conv3D/trainable-weights)

(defonce updates keras.layers.convolutional.Conv3D/updates)

(defonce weights keras.layers.convolutional.Conv3D/weights)
