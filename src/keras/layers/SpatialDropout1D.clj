
(ns layers.SpatialDropout1D
  (:require [keras.layers.core.SpatialDropout1D]))

(defonce SpatialDropout1D keras.layers.core.SpatialDropout1D/SpatialDropout1D)


(defonce add-loss keras.layers.core.SpatialDropout1D/add-loss)

(defonce add-update keras.layers.core.SpatialDropout1D/add-update)

(defonce add-weight keras.layers.core.SpatialDropout1D/add-weight)

(defonce assert-input-compatibility keras.layers.core.SpatialDropout1D/assert-input-compatibility)

(defonce build keras.layers.core.SpatialDropout1D/build)

(defonce built keras.layers.core.SpatialDropout1D/built)

(defonce call keras.layers.core.SpatialDropout1D/call)

(defonce compute-mask keras.layers.core.SpatialDropout1D/compute-mask)

(defonce compute-output-shape keras.layers.core.SpatialDropout1D/compute-output-shape)

(defonce count-params keras.layers.core.SpatialDropout1D/count-params)

(defonce get-config keras.layers.core.SpatialDropout1D/get-config)

(defonce get-input-at keras.layers.core.SpatialDropout1D/get-input-at)

(defonce get-input-mask-at keras.layers.core.SpatialDropout1D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.core.SpatialDropout1D/get-input-shape-at)

(defonce get-losses-for keras.layers.core.SpatialDropout1D/get-losses-for)

(defonce get-output-at keras.layers.core.SpatialDropout1D/get-output-at)

(defonce get-output-mask-at keras.layers.core.SpatialDropout1D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.core.SpatialDropout1D/get-output-shape-at)

(defonce get-updates-for keras.layers.core.SpatialDropout1D/get-updates-for)

(defonce get-weights keras.layers.core.SpatialDropout1D/get-weights)

(defonce input keras.layers.core.SpatialDropout1D/input)

(defonce input-mask keras.layers.core.SpatialDropout1D/input-mask)

(defonce input-shape keras.layers.core.SpatialDropout1D/input-shape)

(defonce losses keras.layers.core.SpatialDropout1D/losses)

(defonce non-trainable-weights keras.layers.core.SpatialDropout1D/non-trainable-weights)

(defonce output keras.layers.core.SpatialDropout1D/output)

(defonce output-mask keras.layers.core.SpatialDropout1D/output-mask)

(defonce output-shape keras.layers.core.SpatialDropout1D/output-shape)

(defonce set-weights keras.layers.core.SpatialDropout1D/set-weights)

(defonce trainable-weights keras.layers.core.SpatialDropout1D/trainable-weights)

(defonce updates keras.layers.core.SpatialDropout1D/updates)

(defonce weights keras.layers.core.SpatialDropout1D/weights)
