
(ns keras.layers.AlphaDropout
  (:require [keras.layers.noise.AlphaDropout]))

(defonce AlphaDropout keras.layers.noise.AlphaDropout/AlphaDropout)


(defonce add-loss keras.layers.noise.AlphaDropout/add-loss)

(defonce add-update keras.layers.noise.AlphaDropout/add-update)

(defonce add-weight keras.layers.noise.AlphaDropout/add-weight)

(defonce assert-input-compatibility keras.layers.noise.AlphaDropout/assert-input-compatibility)

(defonce build keras.layers.noise.AlphaDropout/build)

(defonce built keras.layers.noise.AlphaDropout/built)

(defonce call keras.layers.noise.AlphaDropout/call)

(defonce compute-mask keras.layers.noise.AlphaDropout/compute-mask)

(defonce compute-output-shape keras.layers.noise.AlphaDropout/compute-output-shape)

(defonce count-params keras.layers.noise.AlphaDropout/count-params)

(defonce get-config keras.layers.noise.AlphaDropout/get-config)

(defonce get-input-at keras.layers.noise.AlphaDropout/get-input-at)

(defonce get-input-mask-at keras.layers.noise.AlphaDropout/get-input-mask-at)

(defonce get-input-shape-at keras.layers.noise.AlphaDropout/get-input-shape-at)

(defonce get-losses-for keras.layers.noise.AlphaDropout/get-losses-for)

(defonce get-output-at keras.layers.noise.AlphaDropout/get-output-at)

(defonce get-output-mask-at keras.layers.noise.AlphaDropout/get-output-mask-at)

(defonce get-output-shape-at keras.layers.noise.AlphaDropout/get-output-shape-at)

(defonce get-updates-for keras.layers.noise.AlphaDropout/get-updates-for)

(defonce get-weights keras.layers.noise.AlphaDropout/get-weights)

(defonce input keras.layers.noise.AlphaDropout/input)

(defonce input-mask keras.layers.noise.AlphaDropout/input-mask)

(defonce input-shape keras.layers.noise.AlphaDropout/input-shape)

(defonce losses keras.layers.noise.AlphaDropout/losses)

(defonce non-trainable-weights keras.layers.noise.AlphaDropout/non-trainable-weights)

(defonce output keras.layers.noise.AlphaDropout/output)

(defonce output-mask keras.layers.noise.AlphaDropout/output-mask)

(defonce output-shape keras.layers.noise.AlphaDropout/output-shape)

(defonce set-weights keras.layers.noise.AlphaDropout/set-weights)

(defonce trainable-weights keras.layers.noise.AlphaDropout/trainable-weights)

(defonce updates keras.layers.noise.AlphaDropout/updates)

(defonce weights keras.layers.noise.AlphaDropout/weights)
