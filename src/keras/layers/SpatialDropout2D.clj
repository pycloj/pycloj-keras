
(ns layers.SpatialDropout2D
  (:require [keras.layers.core.SpatialDropout2D]))

(defonce SpatialDropout2D keras.layers.core.SpatialDropout2D/SpatialDropout2D)


(defonce add-loss keras.layers.core.SpatialDropout2D/add-loss)

(defonce add-update keras.layers.core.SpatialDropout2D/add-update)

(defonce add-weight keras.layers.core.SpatialDropout2D/add-weight)

(defonce assert-input-compatibility keras.layers.core.SpatialDropout2D/assert-input-compatibility)

(defonce build keras.layers.core.SpatialDropout2D/build)

(defonce built keras.layers.core.SpatialDropout2D/built)

(defonce call keras.layers.core.SpatialDropout2D/call)

(defonce compute-mask keras.layers.core.SpatialDropout2D/compute-mask)

(defonce compute-output-shape keras.layers.core.SpatialDropout2D/compute-output-shape)

(defonce count-params keras.layers.core.SpatialDropout2D/count-params)

(defonce get-config keras.layers.core.SpatialDropout2D/get-config)

(defonce get-input-at keras.layers.core.SpatialDropout2D/get-input-at)

(defonce get-input-mask-at keras.layers.core.SpatialDropout2D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.core.SpatialDropout2D/get-input-shape-at)

(defonce get-losses-for keras.layers.core.SpatialDropout2D/get-losses-for)

(defonce get-output-at keras.layers.core.SpatialDropout2D/get-output-at)

(defonce get-output-mask-at keras.layers.core.SpatialDropout2D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.core.SpatialDropout2D/get-output-shape-at)

(defonce get-updates-for keras.layers.core.SpatialDropout2D/get-updates-for)

(defonce get-weights keras.layers.core.SpatialDropout2D/get-weights)

(defonce input keras.layers.core.SpatialDropout2D/input)

(defonce input-mask keras.layers.core.SpatialDropout2D/input-mask)

(defonce input-shape keras.layers.core.SpatialDropout2D/input-shape)

(defonce losses keras.layers.core.SpatialDropout2D/losses)

(defonce non-trainable-weights keras.layers.core.SpatialDropout2D/non-trainable-weights)

(defonce output keras.layers.core.SpatialDropout2D/output)

(defonce output-mask keras.layers.core.SpatialDropout2D/output-mask)

(defonce output-shape keras.layers.core.SpatialDropout2D/output-shape)

(defonce set-weights keras.layers.core.SpatialDropout2D/set-weights)

(defonce trainable-weights keras.layers.core.SpatialDropout2D/trainable-weights)

(defonce updates keras.layers.core.SpatialDropout2D/updates)

(defonce weights keras.layers.core.SpatialDropout2D/weights)
