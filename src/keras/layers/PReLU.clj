
(ns layers.PReLU
  (:require [keras.layers.advanced_activations.PReLU]))

(defonce PReLU keras.layers.advanced_activations.PReLU/PReLU)


(defonce add-loss keras.layers.advanced_activations.PReLU/add-loss)

(defonce add-update keras.layers.advanced_activations.PReLU/add-update)

(defonce add-weight keras.layers.advanced_activations.PReLU/add-weight)

(defonce assert-input-compatibility keras.layers.advanced_activations.PReLU/assert-input-compatibility)

(defonce build keras.layers.advanced_activations.PReLU/build)

(defonce built keras.layers.advanced_activations.PReLU/built)

(defonce call keras.layers.advanced_activations.PReLU/call)

(defonce compute-mask keras.layers.advanced_activations.PReLU/compute-mask)

(defonce compute-output-shape keras.layers.advanced_activations.PReLU/compute-output-shape)

(defonce count-params keras.layers.advanced_activations.PReLU/count-params)

(defonce get-config keras.layers.advanced_activations.PReLU/get-config)

(defonce get-input-at keras.layers.advanced_activations.PReLU/get-input-at)

(defonce get-input-mask-at keras.layers.advanced_activations.PReLU/get-input-mask-at)

(defonce get-input-shape-at keras.layers.advanced_activations.PReLU/get-input-shape-at)

(defonce get-losses-for keras.layers.advanced_activations.PReLU/get-losses-for)

(defonce get-output-at keras.layers.advanced_activations.PReLU/get-output-at)

(defonce get-output-mask-at keras.layers.advanced_activations.PReLU/get-output-mask-at)

(defonce get-output-shape-at keras.layers.advanced_activations.PReLU/get-output-shape-at)

(defonce get-updates-for keras.layers.advanced_activations.PReLU/get-updates-for)

(defonce get-weights keras.layers.advanced_activations.PReLU/get-weights)

(defonce input keras.layers.advanced_activations.PReLU/input)

(defonce input-mask keras.layers.advanced_activations.PReLU/input-mask)

(defonce input-shape keras.layers.advanced_activations.PReLU/input-shape)

(defonce losses keras.layers.advanced_activations.PReLU/losses)

(defonce non-trainable-weights keras.layers.advanced_activations.PReLU/non-trainable-weights)

(defonce output keras.layers.advanced_activations.PReLU/output)

(defonce output-mask keras.layers.advanced_activations.PReLU/output-mask)

(defonce output-shape keras.layers.advanced_activations.PReLU/output-shape)

(defonce set-weights keras.layers.advanced_activations.PReLU/set-weights)

(defonce trainable-weights keras.layers.advanced_activations.PReLU/trainable-weights)

(defonce updates keras.layers.advanced_activations.PReLU/updates)

(defonce weights keras.layers.advanced_activations.PReLU/weights)
