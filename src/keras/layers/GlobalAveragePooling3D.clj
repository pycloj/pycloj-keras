
(ns layers.GlobalAveragePooling3D
  (:require [keras.layers.pooling.GlobalAveragePooling3D]))

(defonce GlobalAveragePooling3D keras.layers.pooling.GlobalAveragePooling3D/GlobalAveragePooling3D)


(defonce add-loss keras.layers.pooling.GlobalAveragePooling3D/add-loss)

(defonce add-update keras.layers.pooling.GlobalAveragePooling3D/add-update)

(defonce add-weight keras.layers.pooling.GlobalAveragePooling3D/add-weight)

(defonce assert-input-compatibility keras.layers.pooling.GlobalAveragePooling3D/assert-input-compatibility)

(defonce build keras.layers.pooling.GlobalAveragePooling3D/build)

(defonce built keras.layers.pooling.GlobalAveragePooling3D/built)

(defonce call keras.layers.pooling.GlobalAveragePooling3D/call)

(defonce compute-mask keras.layers.pooling.GlobalAveragePooling3D/compute-mask)

(defonce compute-output-shape keras.layers.pooling.GlobalAveragePooling3D/compute-output-shape)

(defonce count-params keras.layers.pooling.GlobalAveragePooling3D/count-params)

(defonce get-config keras.layers.pooling.GlobalAveragePooling3D/get-config)

(defonce get-input-at keras.layers.pooling.GlobalAveragePooling3D/get-input-at)

(defonce get-input-mask-at keras.layers.pooling.GlobalAveragePooling3D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.pooling.GlobalAveragePooling3D/get-input-shape-at)

(defonce get-losses-for keras.layers.pooling.GlobalAveragePooling3D/get-losses-for)

(defonce get-output-at keras.layers.pooling.GlobalAveragePooling3D/get-output-at)

(defonce get-output-mask-at keras.layers.pooling.GlobalAveragePooling3D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.pooling.GlobalAveragePooling3D/get-output-shape-at)

(defonce get-updates-for keras.layers.pooling.GlobalAveragePooling3D/get-updates-for)

(defonce get-weights keras.layers.pooling.GlobalAveragePooling3D/get-weights)

(defonce input keras.layers.pooling.GlobalAveragePooling3D/input)

(defonce input-mask keras.layers.pooling.GlobalAveragePooling3D/input-mask)

(defonce input-shape keras.layers.pooling.GlobalAveragePooling3D/input-shape)

(defonce losses keras.layers.pooling.GlobalAveragePooling3D/losses)

(defonce non-trainable-weights keras.layers.pooling.GlobalAveragePooling3D/non-trainable-weights)

(defonce output keras.layers.pooling.GlobalAveragePooling3D/output)

(defonce output-mask keras.layers.pooling.GlobalAveragePooling3D/output-mask)

(defonce output-shape keras.layers.pooling.GlobalAveragePooling3D/output-shape)

(defonce set-weights keras.layers.pooling.GlobalAveragePooling3D/set-weights)

(defonce trainable-weights keras.layers.pooling.GlobalAveragePooling3D/trainable-weights)

(defonce updates keras.layers.pooling.GlobalAveragePooling3D/updates)

(defonce weights keras.layers.pooling.GlobalAveragePooling3D/weights)
