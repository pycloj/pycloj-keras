
(ns layers.Concatenate
  (:require [keras.layers.merge.Concatenate]))

(defonce Concatenate keras.layers.merge.Concatenate/Concatenate)


(defonce add-loss keras.layers.merge.Concatenate/add-loss)

(defonce add-update keras.layers.merge.Concatenate/add-update)

(defonce add-weight keras.layers.merge.Concatenate/add-weight)

(defonce assert-input-compatibility keras.layers.merge.Concatenate/assert-input-compatibility)

(defonce build keras.layers.merge.Concatenate/build)

(defonce built keras.layers.merge.Concatenate/built)

(defonce call keras.layers.merge.Concatenate/call)

(defonce compute-mask keras.layers.merge.Concatenate/compute-mask)

(defonce compute-output-shape keras.layers.merge.Concatenate/compute-output-shape)

(defonce count-params keras.layers.merge.Concatenate/count-params)

(defonce get-config keras.layers.merge.Concatenate/get-config)

(defonce get-input-at keras.layers.merge.Concatenate/get-input-at)

(defonce get-input-mask-at keras.layers.merge.Concatenate/get-input-mask-at)

(defonce get-input-shape-at keras.layers.merge.Concatenate/get-input-shape-at)

(defonce get-losses-for keras.layers.merge.Concatenate/get-losses-for)

(defonce get-output-at keras.layers.merge.Concatenate/get-output-at)

(defonce get-output-mask-at keras.layers.merge.Concatenate/get-output-mask-at)

(defonce get-output-shape-at keras.layers.merge.Concatenate/get-output-shape-at)

(defonce get-updates-for keras.layers.merge.Concatenate/get-updates-for)

(defonce get-weights keras.layers.merge.Concatenate/get-weights)

(defonce input keras.layers.merge.Concatenate/input)

(defonce input-mask keras.layers.merge.Concatenate/input-mask)

(defonce input-shape keras.layers.merge.Concatenate/input-shape)

(defonce losses keras.layers.merge.Concatenate/losses)

(defonce non-trainable-weights keras.layers.merge.Concatenate/non-trainable-weights)

(defonce output keras.layers.merge.Concatenate/output)

(defonce output-mask keras.layers.merge.Concatenate/output-mask)

(defonce output-shape keras.layers.merge.Concatenate/output-shape)

(defonce set-weights keras.layers.merge.Concatenate/set-weights)

(defonce trainable-weights keras.layers.merge.Concatenate/trainable-weights)

(defonce updates keras.layers.merge.Concatenate/updates)

(defonce weights keras.layers.merge.Concatenate/weights)
