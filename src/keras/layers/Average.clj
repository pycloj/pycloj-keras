
(ns keras.layers.Average
  (:require [keras.layers.merge.Average]))

(defonce Average keras.layers.merge.Average/Average)


(defonce add-loss keras.layers.merge.Average/add-loss)

(defonce add-update keras.layers.merge.Average/add-update)

(defonce add-weight keras.layers.merge.Average/add-weight)

(defonce assert-input-compatibility keras.layers.merge.Average/assert-input-compatibility)

(defonce build keras.layers.merge.Average/build)

(defonce built keras.layers.merge.Average/built)

(defonce call keras.layers.merge.Average/call)

(defonce compute-mask keras.layers.merge.Average/compute-mask)

(defonce compute-output-shape keras.layers.merge.Average/compute-output-shape)

(defonce count-params keras.layers.merge.Average/count-params)

(defonce get-config keras.layers.merge.Average/get-config)

(defonce get-input-at keras.layers.merge.Average/get-input-at)

(defonce get-input-mask-at keras.layers.merge.Average/get-input-mask-at)

(defonce get-input-shape-at keras.layers.merge.Average/get-input-shape-at)

(defonce get-losses-for keras.layers.merge.Average/get-losses-for)

(defonce get-output-at keras.layers.merge.Average/get-output-at)

(defonce get-output-mask-at keras.layers.merge.Average/get-output-mask-at)

(defonce get-output-shape-at keras.layers.merge.Average/get-output-shape-at)

(defonce get-updates-for keras.layers.merge.Average/get-updates-for)

(defonce get-weights keras.layers.merge.Average/get-weights)

(defonce input keras.layers.merge.Average/input)

(defonce input-mask keras.layers.merge.Average/input-mask)

(defonce input-shape keras.layers.merge.Average/input-shape)

(defonce losses keras.layers.merge.Average/losses)

(defonce non-trainable-weights keras.layers.merge.Average/non-trainable-weights)

(defonce output keras.layers.merge.Average/output)

(defonce output-mask keras.layers.merge.Average/output-mask)

(defonce output-shape keras.layers.merge.Average/output-shape)

(defonce set-weights keras.layers.merge.Average/set-weights)

(defonce trainable-weights keras.layers.merge.Average/trainable-weights)

(defonce updates keras.layers.merge.Average/updates)

(defonce weights keras.layers.merge.Average/weights)
