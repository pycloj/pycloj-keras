
(ns layers.GaussianNoise
  (:require [keras.layers.noise.GaussianNoise]))

(defonce GaussianNoise keras.layers.noise.GaussianNoise/GaussianNoise)


(defonce add-loss keras.layers.noise.GaussianNoise/add-loss)

(defonce add-update keras.layers.noise.GaussianNoise/add-update)

(defonce add-weight keras.layers.noise.GaussianNoise/add-weight)

(defonce assert-input-compatibility keras.layers.noise.GaussianNoise/assert-input-compatibility)

(defonce build keras.layers.noise.GaussianNoise/build)

(defonce built keras.layers.noise.GaussianNoise/built)

(defonce call keras.layers.noise.GaussianNoise/call)

(defonce compute-mask keras.layers.noise.GaussianNoise/compute-mask)

(defonce compute-output-shape keras.layers.noise.GaussianNoise/compute-output-shape)

(defonce count-params keras.layers.noise.GaussianNoise/count-params)

(defonce get-config keras.layers.noise.GaussianNoise/get-config)

(defonce get-input-at keras.layers.noise.GaussianNoise/get-input-at)

(defonce get-input-mask-at keras.layers.noise.GaussianNoise/get-input-mask-at)

(defonce get-input-shape-at keras.layers.noise.GaussianNoise/get-input-shape-at)

(defonce get-losses-for keras.layers.noise.GaussianNoise/get-losses-for)

(defonce get-output-at keras.layers.noise.GaussianNoise/get-output-at)

(defonce get-output-mask-at keras.layers.noise.GaussianNoise/get-output-mask-at)

(defonce get-output-shape-at keras.layers.noise.GaussianNoise/get-output-shape-at)

(defonce get-updates-for keras.layers.noise.GaussianNoise/get-updates-for)

(defonce get-weights keras.layers.noise.GaussianNoise/get-weights)

(defonce input keras.layers.noise.GaussianNoise/input)

(defonce input-mask keras.layers.noise.GaussianNoise/input-mask)

(defonce input-shape keras.layers.noise.GaussianNoise/input-shape)

(defonce losses keras.layers.noise.GaussianNoise/losses)

(defonce non-trainable-weights keras.layers.noise.GaussianNoise/non-trainable-weights)

(defonce output keras.layers.noise.GaussianNoise/output)

(defonce output-mask keras.layers.noise.GaussianNoise/output-mask)

(defonce output-shape keras.layers.noise.GaussianNoise/output-shape)

(defonce set-weights keras.layers.noise.GaussianNoise/set-weights)

(defonce trainable-weights keras.layers.noise.GaussianNoise/trainable-weights)

(defonce updates keras.layers.noise.GaussianNoise/updates)

(defonce weights keras.layers.noise.GaussianNoise/weights)
