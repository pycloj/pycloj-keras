
(ns layers.GlobalMaxPooling2D
  (:require [keras.layers.pooling.GlobalMaxPooling2D]))

(defonce GlobalMaxPooling2D keras.layers.pooling.GlobalMaxPooling2D/GlobalMaxPooling2D)


(defonce add-loss keras.layers.pooling.GlobalMaxPooling2D/add-loss)

(defonce add-update keras.layers.pooling.GlobalMaxPooling2D/add-update)

(defonce add-weight keras.layers.pooling.GlobalMaxPooling2D/add-weight)

(defonce assert-input-compatibility keras.layers.pooling.GlobalMaxPooling2D/assert-input-compatibility)

(defonce build keras.layers.pooling.GlobalMaxPooling2D/build)

(defonce built keras.layers.pooling.GlobalMaxPooling2D/built)

(defonce call keras.layers.pooling.GlobalMaxPooling2D/call)

(defonce compute-mask keras.layers.pooling.GlobalMaxPooling2D/compute-mask)

(defonce compute-output-shape keras.layers.pooling.GlobalMaxPooling2D/compute-output-shape)

(defonce count-params keras.layers.pooling.GlobalMaxPooling2D/count-params)

(defonce get-config keras.layers.pooling.GlobalMaxPooling2D/get-config)

(defonce get-input-at keras.layers.pooling.GlobalMaxPooling2D/get-input-at)

(defonce get-input-mask-at keras.layers.pooling.GlobalMaxPooling2D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.pooling.GlobalMaxPooling2D/get-input-shape-at)

(defonce get-losses-for keras.layers.pooling.GlobalMaxPooling2D/get-losses-for)

(defonce get-output-at keras.layers.pooling.GlobalMaxPooling2D/get-output-at)

(defonce get-output-mask-at keras.layers.pooling.GlobalMaxPooling2D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.pooling.GlobalMaxPooling2D/get-output-shape-at)

(defonce get-updates-for keras.layers.pooling.GlobalMaxPooling2D/get-updates-for)

(defonce get-weights keras.layers.pooling.GlobalMaxPooling2D/get-weights)

(defonce input keras.layers.pooling.GlobalMaxPooling2D/input)

(defonce input-mask keras.layers.pooling.GlobalMaxPooling2D/input-mask)

(defonce input-shape keras.layers.pooling.GlobalMaxPooling2D/input-shape)

(defonce losses keras.layers.pooling.GlobalMaxPooling2D/losses)

(defonce non-trainable-weights keras.layers.pooling.GlobalMaxPooling2D/non-trainable-weights)

(defonce output keras.layers.pooling.GlobalMaxPooling2D/output)

(defonce output-mask keras.layers.pooling.GlobalMaxPooling2D/output-mask)

(defonce output-shape keras.layers.pooling.GlobalMaxPooling2D/output-shape)

(defonce set-weights keras.layers.pooling.GlobalMaxPooling2D/set-weights)

(defonce trainable-weights keras.layers.pooling.GlobalMaxPooling2D/trainable-weights)

(defonce updates keras.layers.pooling.GlobalMaxPooling2D/updates)

(defonce weights keras.layers.pooling.GlobalMaxPooling2D/weights)
