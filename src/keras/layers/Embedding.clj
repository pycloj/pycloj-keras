
(ns layers.Embedding
  (:require [keras.layers.embeddings.Embedding]))

(defonce Embedding keras.layers.embeddings.Embedding/Embedding)


(defonce add-loss keras.layers.embeddings.Embedding/add-loss)

(defonce add-update keras.layers.embeddings.Embedding/add-update)

(defonce add-weight keras.layers.embeddings.Embedding/add-weight)

(defonce assert-input-compatibility keras.layers.embeddings.Embedding/assert-input-compatibility)

(defonce build keras.layers.embeddings.Embedding/build)

(defonce built keras.layers.embeddings.Embedding/built)

(defonce call keras.layers.embeddings.Embedding/call)

(defonce compute-mask keras.layers.embeddings.Embedding/compute-mask)

(defonce compute-output-shape keras.layers.embeddings.Embedding/compute-output-shape)

(defonce count-params keras.layers.embeddings.Embedding/count-params)

(defonce get-config keras.layers.embeddings.Embedding/get-config)

(defonce get-input-at keras.layers.embeddings.Embedding/get-input-at)

(defonce get-input-mask-at keras.layers.embeddings.Embedding/get-input-mask-at)

(defonce get-input-shape-at keras.layers.embeddings.Embedding/get-input-shape-at)

(defonce get-losses-for keras.layers.embeddings.Embedding/get-losses-for)

(defonce get-output-at keras.layers.embeddings.Embedding/get-output-at)

(defonce get-output-mask-at keras.layers.embeddings.Embedding/get-output-mask-at)

(defonce get-output-shape-at keras.layers.embeddings.Embedding/get-output-shape-at)

(defonce get-updates-for keras.layers.embeddings.Embedding/get-updates-for)

(defonce get-weights keras.layers.embeddings.Embedding/get-weights)

(defonce input keras.layers.embeddings.Embedding/input)

(defonce input-mask keras.layers.embeddings.Embedding/input-mask)

(defonce input-shape keras.layers.embeddings.Embedding/input-shape)

(defonce losses keras.layers.embeddings.Embedding/losses)

(defonce non-trainable-weights keras.layers.embeddings.Embedding/non-trainable-weights)

(defonce output keras.layers.embeddings.Embedding/output)

(defonce output-mask keras.layers.embeddings.Embedding/output-mask)

(defonce output-shape keras.layers.embeddings.Embedding/output-shape)

(defonce set-weights keras.layers.embeddings.Embedding/set-weights)

(defonce trainable-weights keras.layers.embeddings.Embedding/trainable-weights)

(defonce updates keras.layers.embeddings.Embedding/updates)

(defonce weights keras.layers.embeddings.Embedding/weights)
