
(ns layers.MaxPooling3D
  (:require [keras.layers.pooling.MaxPooling3D]))

(defonce MaxPooling3D keras.layers.pooling.MaxPooling3D/MaxPooling3D)


(defonce add-loss keras.layers.pooling.MaxPooling3D/add-loss)

(defonce add-update keras.layers.pooling.MaxPooling3D/add-update)

(defonce add-weight keras.layers.pooling.MaxPooling3D/add-weight)

(defonce assert-input-compatibility keras.layers.pooling.MaxPooling3D/assert-input-compatibility)

(defonce build keras.layers.pooling.MaxPooling3D/build)

(defonce built keras.layers.pooling.MaxPooling3D/built)

(defonce call keras.layers.pooling.MaxPooling3D/call)

(defonce compute-mask keras.layers.pooling.MaxPooling3D/compute-mask)

(defonce compute-output-shape keras.layers.pooling.MaxPooling3D/compute-output-shape)

(defonce count-params keras.layers.pooling.MaxPooling3D/count-params)

(defonce get-config keras.layers.pooling.MaxPooling3D/get-config)

(defonce get-input-at keras.layers.pooling.MaxPooling3D/get-input-at)

(defonce get-input-mask-at keras.layers.pooling.MaxPooling3D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.pooling.MaxPooling3D/get-input-shape-at)

(defonce get-losses-for keras.layers.pooling.MaxPooling3D/get-losses-for)

(defonce get-output-at keras.layers.pooling.MaxPooling3D/get-output-at)

(defonce get-output-mask-at keras.layers.pooling.MaxPooling3D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.pooling.MaxPooling3D/get-output-shape-at)

(defonce get-updates-for keras.layers.pooling.MaxPooling3D/get-updates-for)

(defonce get-weights keras.layers.pooling.MaxPooling3D/get-weights)

(defonce input keras.layers.pooling.MaxPooling3D/input)

(defonce input-mask keras.layers.pooling.MaxPooling3D/input-mask)

(defonce input-shape keras.layers.pooling.MaxPooling3D/input-shape)

(defonce losses keras.layers.pooling.MaxPooling3D/losses)

(defonce non-trainable-weights keras.layers.pooling.MaxPooling3D/non-trainable-weights)

(defonce output keras.layers.pooling.MaxPooling3D/output)

(defonce output-mask keras.layers.pooling.MaxPooling3D/output-mask)

(defonce output-shape keras.layers.pooling.MaxPooling3D/output-shape)

(defonce set-weights keras.layers.pooling.MaxPooling3D/set-weights)

(defonce trainable-weights keras.layers.pooling.MaxPooling3D/trainable-weights)

(defonce updates keras.layers.pooling.MaxPooling3D/updates)

(defonce weights keras.layers.pooling.MaxPooling3D/weights)
