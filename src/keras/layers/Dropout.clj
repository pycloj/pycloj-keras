
(ns layers.Dropout
  (:require [keras.layers.core.Dropout]))

(defonce Dropout keras.layers.core.Dropout/Dropout)


(defonce add-loss keras.layers.core.Dropout/add-loss)

(defonce add-update keras.layers.core.Dropout/add-update)

(defonce add-weight keras.layers.core.Dropout/add-weight)

(defonce assert-input-compatibility keras.layers.core.Dropout/assert-input-compatibility)

(defonce build keras.layers.core.Dropout/build)

(defonce built keras.layers.core.Dropout/built)

(defonce call keras.layers.core.Dropout/call)

(defonce compute-mask keras.layers.core.Dropout/compute-mask)

(defonce compute-output-shape keras.layers.core.Dropout/compute-output-shape)

(defonce count-params keras.layers.core.Dropout/count-params)

(defonce get-config keras.layers.core.Dropout/get-config)

(defonce get-input-at keras.layers.core.Dropout/get-input-at)

(defonce get-input-mask-at keras.layers.core.Dropout/get-input-mask-at)

(defonce get-input-shape-at keras.layers.core.Dropout/get-input-shape-at)

(defonce get-losses-for keras.layers.core.Dropout/get-losses-for)

(defonce get-output-at keras.layers.core.Dropout/get-output-at)

(defonce get-output-mask-at keras.layers.core.Dropout/get-output-mask-at)

(defonce get-output-shape-at keras.layers.core.Dropout/get-output-shape-at)

(defonce get-updates-for keras.layers.core.Dropout/get-updates-for)

(defonce get-weights keras.layers.core.Dropout/get-weights)

(defonce input keras.layers.core.Dropout/input)

(defonce input-mask keras.layers.core.Dropout/input-mask)

(defonce input-shape keras.layers.core.Dropout/input-shape)

(defonce losses keras.layers.core.Dropout/losses)

(defonce non-trainable-weights keras.layers.core.Dropout/non-trainable-weights)

(defonce output keras.layers.core.Dropout/output)

(defonce output-mask keras.layers.core.Dropout/output-mask)

(defonce output-shape keras.layers.core.Dropout/output-shape)

(defonce set-weights keras.layers.core.Dropout/set-weights)

(defonce trainable-weights keras.layers.core.Dropout/trainable-weights)

(defonce updates keras.layers.core.Dropout/updates)

(defonce weights keras.layers.core.Dropout/weights)
