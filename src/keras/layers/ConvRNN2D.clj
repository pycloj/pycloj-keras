
(ns layers.ConvRNN2D
  (:require [keras.layers.convolutional_recurrent.ConvRNN2D]))

(defonce ConvRNN2D keras.layers.convolutional_recurrent.ConvRNN2D/ConvRNN2D)


(defonce add-loss keras.layers.convolutional_recurrent.ConvRNN2D/add-loss)

(defonce add-update keras.layers.convolutional_recurrent.ConvRNN2D/add-update)

(defonce add-weight keras.layers.convolutional_recurrent.ConvRNN2D/add-weight)

(defonce assert-input-compatibility keras.layers.convolutional_recurrent.ConvRNN2D/assert-input-compatibility)

(defonce build keras.layers.convolutional_recurrent.ConvRNN2D/build)

(defonce built keras.layers.convolutional_recurrent.ConvRNN2D/built)

(defonce call keras.layers.convolutional_recurrent.ConvRNN2D/call)

(defonce compute-mask keras.layers.convolutional_recurrent.ConvRNN2D/compute-mask)

(defonce compute-output-shape keras.layers.convolutional_recurrent.ConvRNN2D/compute-output-shape)

(defonce count-params keras.layers.convolutional_recurrent.ConvRNN2D/count-params)

(defonce get-config keras.layers.convolutional_recurrent.ConvRNN2D/get-config)

(defonce get-initial-state keras.layers.convolutional_recurrent.ConvRNN2D/get-initial-state)

(defonce get-input-at keras.layers.convolutional_recurrent.ConvRNN2D/get-input-at)

(defonce get-input-mask-at keras.layers.convolutional_recurrent.ConvRNN2D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.convolutional_recurrent.ConvRNN2D/get-input-shape-at)

(defonce get-losses-for keras.layers.convolutional_recurrent.ConvRNN2D/get-losses-for)

(defonce get-output-at keras.layers.convolutional_recurrent.ConvRNN2D/get-output-at)

(defonce get-output-mask-at keras.layers.convolutional_recurrent.ConvRNN2D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.convolutional_recurrent.ConvRNN2D/get-output-shape-at)

(defonce get-updates-for keras.layers.convolutional_recurrent.ConvRNN2D/get-updates-for)

(defonce get-weights keras.layers.convolutional_recurrent.ConvRNN2D/get-weights)

(defonce input keras.layers.convolutional_recurrent.ConvRNN2D/input)

(defonce input-mask keras.layers.convolutional_recurrent.ConvRNN2D/input-mask)

(defonce input-shape keras.layers.convolutional_recurrent.ConvRNN2D/input-shape)

(defonce losses keras.layers.convolutional_recurrent.ConvRNN2D/losses)

(defonce non-trainable-weights keras.layers.convolutional_recurrent.ConvRNN2D/non-trainable-weights)

(defonce output keras.layers.convolutional_recurrent.ConvRNN2D/output)

(defonce output-mask keras.layers.convolutional_recurrent.ConvRNN2D/output-mask)

(defonce output-shape keras.layers.convolutional_recurrent.ConvRNN2D/output-shape)

(defonce reset-states keras.layers.convolutional_recurrent.ConvRNN2D/reset-states)

(defonce set-weights keras.layers.convolutional_recurrent.ConvRNN2D/set-weights)

(defonce states keras.layers.convolutional_recurrent.ConvRNN2D/states)

(defonce trainable-weights keras.layers.convolutional_recurrent.ConvRNN2D/trainable-weights)

(defonce updates keras.layers.convolutional_recurrent.ConvRNN2D/updates)

(defonce weights keras.layers.convolutional_recurrent.ConvRNN2D/weights)
