
(ns keras.layers.AveragePooling1D
  (:require [keras.layers.pooling.AveragePooling1D]))

(defonce AveragePooling1D keras.layers.pooling.AveragePooling1D/AveragePooling1D)


(defonce add-loss keras.layers.pooling.AveragePooling1D/add-loss)

(defonce add-update keras.layers.pooling.AveragePooling1D/add-update)

(defonce add-weight keras.layers.pooling.AveragePooling1D/add-weight)

(defonce assert-input-compatibility keras.layers.pooling.AveragePooling1D/assert-input-compatibility)

(defonce build keras.layers.pooling.AveragePooling1D/build)

(defonce built keras.layers.pooling.AveragePooling1D/built)

(defonce call keras.layers.pooling.AveragePooling1D/call)

(defonce compute-mask keras.layers.pooling.AveragePooling1D/compute-mask)

(defonce compute-output-shape keras.layers.pooling.AveragePooling1D/compute-output-shape)

(defonce count-params keras.layers.pooling.AveragePooling1D/count-params)

(defonce get-config keras.layers.pooling.AveragePooling1D/get-config)

(defonce get-input-at keras.layers.pooling.AveragePooling1D/get-input-at)

(defonce get-input-mask-at keras.layers.pooling.AveragePooling1D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.pooling.AveragePooling1D/get-input-shape-at)

(defonce get-losses-for keras.layers.pooling.AveragePooling1D/get-losses-for)

(defonce get-output-at keras.layers.pooling.AveragePooling1D/get-output-at)

(defonce get-output-mask-at keras.layers.pooling.AveragePooling1D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.pooling.AveragePooling1D/get-output-shape-at)

(defonce get-updates-for keras.layers.pooling.AveragePooling1D/get-updates-for)

(defonce get-weights keras.layers.pooling.AveragePooling1D/get-weights)

(defonce input keras.layers.pooling.AveragePooling1D/input)

(defonce input-mask keras.layers.pooling.AveragePooling1D/input-mask)

(defonce input-shape keras.layers.pooling.AveragePooling1D/input-shape)

(defonce losses keras.layers.pooling.AveragePooling1D/losses)

(defonce non-trainable-weights keras.layers.pooling.AveragePooling1D/non-trainable-weights)

(defonce output keras.layers.pooling.AveragePooling1D/output)

(defonce output-mask keras.layers.pooling.AveragePooling1D/output-mask)

(defonce output-shape keras.layers.pooling.AveragePooling1D/output-shape)

(defonce set-weights keras.layers.pooling.AveragePooling1D/set-weights)

(defonce trainable-weights keras.layers.pooling.AveragePooling1D/trainable-weights)

(defonce updates keras.layers.pooling.AveragePooling1D/updates)

(defonce weights keras.layers.pooling.AveragePooling1D/weights)
