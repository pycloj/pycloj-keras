
(ns layers.UpSampling3D
  (:require [keras.layers.convolutional.UpSampling3D]))

(defonce UpSampling3D keras.layers.convolutional.UpSampling3D/UpSampling3D)


(defonce add-loss keras.layers.convolutional.UpSampling3D/add-loss)

(defonce add-update keras.layers.convolutional.UpSampling3D/add-update)

(defonce add-weight keras.layers.convolutional.UpSampling3D/add-weight)

(defonce assert-input-compatibility keras.layers.convolutional.UpSampling3D/assert-input-compatibility)

(defonce build keras.layers.convolutional.UpSampling3D/build)

(defonce built keras.layers.convolutional.UpSampling3D/built)

(defonce call keras.layers.convolutional.UpSampling3D/call)

(defonce compute-mask keras.layers.convolutional.UpSampling3D/compute-mask)

(defonce compute-output-shape keras.layers.convolutional.UpSampling3D/compute-output-shape)

(defonce count-params keras.layers.convolutional.UpSampling3D/count-params)

(defonce get-config keras.layers.convolutional.UpSampling3D/get-config)

(defonce get-input-at keras.layers.convolutional.UpSampling3D/get-input-at)

(defonce get-input-mask-at keras.layers.convolutional.UpSampling3D/get-input-mask-at)

(defonce get-input-shape-at keras.layers.convolutional.UpSampling3D/get-input-shape-at)

(defonce get-losses-for keras.layers.convolutional.UpSampling3D/get-losses-for)

(defonce get-output-at keras.layers.convolutional.UpSampling3D/get-output-at)

(defonce get-output-mask-at keras.layers.convolutional.UpSampling3D/get-output-mask-at)

(defonce get-output-shape-at keras.layers.convolutional.UpSampling3D/get-output-shape-at)

(defonce get-updates-for keras.layers.convolutional.UpSampling3D/get-updates-for)

(defonce get-weights keras.layers.convolutional.UpSampling3D/get-weights)

(defonce input keras.layers.convolutional.UpSampling3D/input)

(defonce input-mask keras.layers.convolutional.UpSampling3D/input-mask)

(defonce input-shape keras.layers.convolutional.UpSampling3D/input-shape)

(defonce losses keras.layers.convolutional.UpSampling3D/losses)

(defonce non-trainable-weights keras.layers.convolutional.UpSampling3D/non-trainable-weights)

(defonce output keras.layers.convolutional.UpSampling3D/output)

(defonce output-mask keras.layers.convolutional.UpSampling3D/output-mask)

(defonce output-shape keras.layers.convolutional.UpSampling3D/output-shape)

(defonce set-weights keras.layers.convolutional.UpSampling3D/set-weights)

(defonce trainable-weights keras.layers.convolutional.UpSampling3D/trainable-weights)

(defonce updates keras.layers.convolutional.UpSampling3D/updates)

(defonce weights keras.layers.convolutional.UpSampling3D/weights)
