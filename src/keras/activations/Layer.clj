
(ns keras.activations.Layer
  (:require [keras.engine.base_layer.Layer]))

(defonce Layer keras.engine.base_layer.Layer/Layer)


(defonce add-loss keras.engine.base_layer.Layer/add-loss)

(defonce add-update keras.engine.base_layer.Layer/add-update)

(defonce add-weight keras.engine.base_layer.Layer/add-weight)

(defonce assert-input-compatibility keras.engine.base_layer.Layer/assert-input-compatibility)

(defonce build keras.engine.base_layer.Layer/build)

(defonce built keras.engine.base_layer.Layer/built)

(defonce call keras.engine.base_layer.Layer/call)

(defonce compute-mask keras.engine.base_layer.Layer/compute-mask)

(defonce compute-output-shape keras.engine.base_layer.Layer/compute-output-shape)

(defonce count-params keras.engine.base_layer.Layer/count-params)

(defonce get-config keras.engine.base_layer.Layer/get-config)

(defonce get-input-at keras.engine.base_layer.Layer/get-input-at)

(defonce get-input-mask-at keras.engine.base_layer.Layer/get-input-mask-at)

(defonce get-input-shape-at keras.engine.base_layer.Layer/get-input-shape-at)

(defonce get-losses-for keras.engine.base_layer.Layer/get-losses-for)

(defonce get-output-at keras.engine.base_layer.Layer/get-output-at)

(defonce get-output-mask-at keras.engine.base_layer.Layer/get-output-mask-at)

(defonce get-output-shape-at keras.engine.base_layer.Layer/get-output-shape-at)

(defonce get-updates-for keras.engine.base_layer.Layer/get-updates-for)

(defonce get-weights keras.engine.base_layer.Layer/get-weights)

(defonce input keras.engine.base_layer.Layer/input)

(defonce input-mask keras.engine.base_layer.Layer/input-mask)

(defonce input-shape keras.engine.base_layer.Layer/input-shape)

(defonce losses keras.engine.base_layer.Layer/losses)

(defonce non-trainable-weights keras.engine.base_layer.Layer/non-trainable-weights)

(defonce output keras.engine.base_layer.Layer/output)

(defonce output-mask keras.engine.base_layer.Layer/output-mask)

(defonce output-shape keras.engine.base_layer.Layer/output-shape)

(defonce set-weights keras.engine.base_layer.Layer/set-weights)

(defonce trainable-weights keras.engine.base_layer.Layer/trainable-weights)

(defonce updates keras.engine.base_layer.Layer/updates)

(defonce weights keras.engine.base_layer.Layer/weights)
